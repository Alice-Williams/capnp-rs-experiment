manifest_version = 1
updated = "2026-08-31"

[project]
license = "MIT"

[upstream.capnproto]
role = "primary product and protocol oracle"
commit = "e7c9cd96f1505b5ae486db7821006c2f5dce5b5b"

[[upstream.capnproto.schemas]]
name = "schema.capnp"
url = "https://raw.githubusercontent.com/capnproto/capnproto/e7c9cd96f1505b5ae486db7821006c2f5dce5b5b/c%2B%2B/src/capnp/schema.capnp"
sha256 = "3557ff301cec23ef90f59ba2265a741f8fe59ba63bf0e5d12b1619cfc8d74c8d"

[[upstream.capnproto.schemas]]
name = "rpc.capnp"
url = "https://raw.githubusercontent.com/capnproto/capnproto/e7c9cd96f1505b5ae486db7821006c2f5dce5b5b/c%2B%2B/src/capnp/rpc.capnp"
sha256 = "2ecc3049d4f7f2d48a3a368dbb9ef4b97b31c1365996d615bd19c267983a1931"

[[upstream.capnproto.schemas]]
name = "rpc-twoparty.capnp"
url = "https://raw.githubusercontent.com/capnproto/capnproto/e7c9cd96f1505b5ae486db7821006c2f5dce5b5b/c%2B%2B/src/capnp/rpc-twoparty.capnp"
sha256 = "22680f70c56e3c44dc73b52bf8dfd2838a5ea44249be01609be2d362d308b518"

[[upstream.capnproto.schemas]]
name = "persistent.capnp"
url = "https://raw.githubusercontent.com/capnproto/capnproto/e7c9cd96f1505b5ae486db7821006c2f5dce5b5b/c%2B%2B/src/capnp/persistent.capnp"
sha256 = "d77d4d2e2c1e9c42ded13de54ed11b535b076ac210945b53ecf76fd7648a867a"

[[upstream.capnproto.schemas]]
name = "stream.capnp"
url = "https://raw.githubusercontent.com/capnproto/capnproto/e7c9cd96f1505b5ae486db7821006c2f5dce5b5b/c%2B%2B/src/capnp/stream.capnp"
sha256 = "5b0656ca3daca9ef28740c14813d5dd474fd0f9991ce99f652838f4cccf6fb30"

[[upstream.capnproto.schemas]]
name = "c++.capnp"
url = "https://raw.githubusercontent.com/capnproto/capnproto/e7c9cd96f1505b5ae486db7821006c2f5dce5b5b/c%2B%2B/src/capnp/c%2B%2B.capnp"
sha256 = "52ead0eaa560b21e692d6f3d9966a52dfc99e19f53ae2d7c7bd2944a240d86de"

[upstream.capnproto_rust]
role = "secondary regression and Rust ergonomics oracle"
commit = "2228b71e55cee819c30450bb9bfd9c1f6a722429"

[toolchain]
msrv = "1.85.0"
development_rust = "1.98.0"
rust_edition = "2024"
bazel = "9.2.0"
bazelisk = "1.29.0"
rules_rust = "0.74.0"
rules_shell = "0.8.0"

[support]
wire = "M10 borrowed and Arc-owned typed readers with stable coordinate references"
generated_api = "M21 typed data and thread-safe local interface APIs with exact pipelines"
native_compiler = "M26 native multi-file compile and id CLI, plugin-compatible request streaming, deterministic built-in Rust output, and self-hosted generation without a system capnp compiler"
rpc_level = "M21 in-memory local dispatch, inheritance, streaming completion, and capability pipelines; network protocol deferred to M32+"
parallel_read = "not-started"
parallel_build = "not-started"
std = "M16 sync partial-I/O and executor-neutral async framing adapters"
no_std_alloc = "M16 capnp-message and capnp-io alloc feature matrix"
no_allocation = "M16 capnp-wire plus caller-segment-buffer frame parsing"
construction = "M14 bounded schema-independent canonicalization and exact canonical checking"
packed = "M15 bounded incremental encoder and decoder with pinned C++ byte parity"

[milestones]
M00 = "complete"
M01 = "complete"
M02 = "complete"
M03 = "complete"
M04 = "complete"
M05 = "complete"
M06 = "complete"
M07 = "complete"
M08 = "complete"
M09 = "complete"
M10 = "complete"
M11 = "complete"
M12 = "complete"
M13 = "complete"
M14 = "complete"
M15 = "complete"
M16 = "complete"
M17 = "complete"
M18 = "complete"
M19 = "complete"
M20 = "complete"
M21 = "complete"
M22 = "complete"
M23 = "complete"
M24 = "complete"
M25 = "complete"
M26 = "complete"

[evidence.M26]
crate = "crates/capnp-cli"
commands = ["capnp-cli id", "capnp-cli compile --src-prefix DIR [-I DIR]... -o TARGET FILE..."]
workflows = ["recursive relative and rooted import discovery", "multiple requested files merged into one standard CodeGeneratorRequest", "raw unpacked request output", "built-in deterministic Rust generation", "external generator plugins receiving the request on stdin in their output directory"]
paths = ["stable source-prefix-relative virtual paths", "ordered import roots", "bundled pinned /capnp/c++.capnp and /capnp/stream.capnp fallback", "source-prefix escape rejection", "generated-output collision rejection"]
mappings = "repeatable --import-map ID=RUST_PATH values feed the native Rust generator and reject duplicate IDs"
id = "reads eight bytes from Linux /dev/urandom, sets the Cap'n Proto high bit, prints @0x...;, and surfaces open or short-read entropy failures"
negative = ["missing entries and imports", "paths outside the source prefix", "duplicate import mappings", "conflicting duplicate request nodes", "unsafe or unavailable plugin names", "plugin spawn, stdin, exit, and output errors"]
replacement = "docs/tooling/compile-and-id.md documents Cargo/Bazel use, raw/plugin targets, import behavior, migration from capnp compile/capnpc, and the Linux Dev Container boundary"
validation = "Rust 1.98 and 1.85 workspace tests; public API docs and doc tests; Clippy -D warnings; real id and two-entry Rust generation smoke test; Bazel 18/18"

[evidence.M25]
crates = ["crates/capnp-compiler", "crates/capnp-schema", "crates/capnp-message", "crates/capnp-codegen"]
bootstrap = "the pinned schema.capnp CodeGeneratorRequest is checked in as reflection data; native compilation of the pinned schema source reproduces its semantic model, emits a reloadable standard request, and generates byte-identical canonical Rust"
native_model = ["files, structs, groups, enums, constants, annotations, interfaces, methods, parameters and results", "explicit, child, group, and method IDs", "types, brands, generics, annotations, superclasses, imports, source information, and identifiers", "scalar and owned aggregate constants", "streaming result metadata"]
oracle = "native models agree with all eight pinned C++ CodeGeneratorRequest fixtures; schema.capnp exercises nested unions, qualified identifiers, primitive aliases, documentation, standard annotations, and complete reflection metadata"
serialization = "all pinned requests serialize through native dynamic builders, reload under bounded validation, and preserve semantic nodes and requested-file metadata"
self_hosting = "generated fixture crates now resolve .capnp sources, construct native requests, and run Rust code generation during clean Cargo builds without invoking capnp"
determinism = ["canonical code-generation node identity order", "repeat generation equality", "repeat native request emission equality", "native and reloaded request generation equality"]
compatibility_fixes = ["aggregate pointer graph copying", "applied generic member expressions", "keyword-named fields and groups", "nested constants and constant references", "implicit layout IDs", "nested-union allocation through containing arms", "union padding ownership", "standard stream and C++ annotation modules"]
validation = "Rust 1.98 and 1.85 workspace tests; public API docs; Clippy -D warnings; fixture hashes; Bazel complete test suite"

[evidence.M24]
crate = "crates/capnp-compiler"
allocator = ["ordinal-order field allocation", "power-of-two aligned data slots", "smallest-hole-first padding reuse", "separate pointer section", "shared union data and pointer lanes", "16-bit discriminants", "normal groups sharing parent storage"]
nodes = ["root and group struct sizes", "field offsets and code order", "explicit and implicit ordinals", "named and anonymous unions", "union group arms", "preferred inline-composite list encoding", "deterministic MD5-derived group IDs"]
oracle = "every struct/group field count, ID, data-word count, pointer count, field offset, code order, ordinal, discriminant value/count/offset, and group flag agrees with pinned C++ compiler requests for wire-fixture and evolution-v1/v2/v3"
evolution = "all field ordinals present in an earlier evolution fixture retain the same data or pointer offset in later fixtures, including fields moved into a union group"
negative = ["missing and non-sequential field ordinals", "empty groups", "unions with fewer than two arms", "late explicit union ordinals", "unresolvable layout types", "data, pointer, and union-count schema limits"]
additional = ["smallest power-of-two padding reuse", "anonymous union flattening", "group-arm lane sharing", "pinned deterministic group-ID vector"]
scope = "compiled layout model only; CodeGeneratorRequest wire emission, native CLI integration, and self-hosted code generation remain M25-M28"
validation = "Rust 1.98 and 1.85 workspace tests; public API docs; Clippy -D warnings; four pinned C++ compiler-request layout comparisons; Bazel 18/18"

[evidence.M23]
crate = "crates/capnp-compiler"
model = ["explicit and overridable standard module sources", "normalized relative and absolute imports", "lexically scoped import and declaration aliases", "file and declaration IDs", "field, enumerant, and method ordinals", "generic declaration and method scopes", "resolved type and value expressions", "annotation declarations, targets, and applications"]
determinism = "BTreeMap/BTreeSet module discovery, symbol indexes, diagnostics, and duplicate checks produce identical output for reversed source insertion order"
oracle = "native language/import fixture file IDs, explicit node IDs, declaration names and kinds, generic parameters, import IDs, and constant values agree with pinned C++ CodeGeneratorRequest fixtures at e7c9cd96f1505b5ae486db7821006c2f5dce5b5b"
overrides = "an explicit /capnp/base.capnp source wins over the standard source with identical results in either insertion order"
negative = ["missing modules and names", "duplicate names, ordinals, imports, and global IDs", "invalid high-bit IDs", "constant and alias cycles", "invalid annotation targets", "module, declaration, and expression work limits"]
scope = "semantic resolution only; field layout, discriminants, evolution layout, and CodeGeneratorRequest emission remain M24-M25"
validation = "Rust 1.98 and 1.85 workspace tests; public API docs; Clippy -D warnings; pinned C++ compiler-request comparisons; Bazel 18/18"

[evidence.M22]
crate = "crates/capnp-compiler"
oracle_source = "pinned C++ compiler lexer.c++, lexer.capnp, lexer-test.c++, and parser declaration grammar at e7c9cd96f1505b5ae486db7821006c2f5dce5b5b"
syntax = ["identifiers", "decoded strings and backtick lines", "hex binary", "integer and float literals", "operators", "nested comma-delimited parentheses and brackets", "semicolon declarations", "brace blocks", "UTF-8 BOM", "post-declaration documentation comments"]
lossless = ["Arc-owned original UTF-8 source", "half-open byte ranges on tokens and statements", "whitespace, comment, and BOM trivia ranges", "exact source slicing"]
limits = ["source bytes before scanning", "tokens", "statements", "shared bracket and block nesting", "retained diagnostics"]
recovery = ["invalid character tokens", "missing list delimiters", "missing statement terminators", "unmatched braces", "unterminated strings and binary literals", "overflowing numbers"]
oracle_check = "tools/verify-m22-parser.sh: native and pinned C++ agree on 14 accepted local/upstream schemas and 4 syntax rejections"
fuzz_regression = "deterministic arbitrary byte prefixes through length 511 never panic; successful UTF-8 parses retain bounded diagnostics and at most linear recorded cursor work; 100000-level nesting is rejected at the configured depth"
scope = "lossless lexical/statement syntax only; name, import, ID, constant, type, and layout semantics remain M23-M25"
validation = "Rust 1.98 and 1.85 workspace tests; public API docs; Clippy -D warnings; pinned C++ accept/reject harness; Bazel complete test suite"

[evidence.M21]
crates = ["crates/capnp-codegen", "crates/capnp-rpc", "crates/capnp-generated-fixture", "crates/capnp-generated-import-fixture"]
interfaces = ["thread-safe typed clients", "Send + Sync server traits with Send response futures", "typed request/result readers", "generic interface and method parameters", "default unimplemented methods"]
inheritance = "GenericService(String) local adapter dispatches BaseService.ping through the inherited interface ID and returns the typed result"
pipelines = ["generated struct pipeline types", "exact pointer-field transform vectors", "typed capability pipelines", "nested runtime transform coverage", "open().stream resolves capability 42 at pointer slot 0"]
streaming = "generated StreamingCall exposes completion backpressure; local handler remains unexecuted until completion is polled"
oracle_fixtures = ["compiler-request-language-fixture.bin", "compiler-request-streaming-fixture.bin"]
scope = "in-memory local transport only; network RPC protocol tables, vats, cancellation, embargo, and reconnect behavior remain M32+"
validation = "Rust 1.98 and 1.85 workspace tests; generated-module and cross-crate compilation; public API docs; Clippy -D warnings; Bazel complete test suite"

[evidence.M20]
crates = ["crates/capnp-codegen", "crates/capnp-generated-fixture", "crates/capnp-generated-import-fixture"]
generics = ["inherited outer and inner scope parameters", "concrete runtime brands from Rust type parameters", "typed Box(String), Box(Vec<u8>), and Pair(String, Vec<u8>) reads", "typed generic builder setters", "lossless unbound DynamicValue fallback"]
constants = ["scalar Rust constants", "Text and Data Rust constants", "owned list pointer accessors", "owned branded struct pointer accessors"]
annotations = ["typed value decoder", "annotation ID check", "exact target metadata", "lookup over applied annotations"]
imports = "file-ID-to-Rust-path map compiled and exercised across separate generated crates"
oracle = "typed generic APIs decode the pinned C++ language fixture"
compile_growth = "benchmarks/results/2026-08-30-g-drive-docker/m20-compile-growth.tsv: 105794-846352 generated bytes checked in 761-2603 ms after dependency warmup"
scope = "interface clients, servers, methods, inheritance, and pipelines remain M21"
validation = "Rust 1.98 and 1.85 workspace tests; generated-module and cross-crate compilation; public API docs; Clippy -D warnings; Bazel complete test suite"

[evidence.M19]
crates = ["crates/capnp-codegen", "crates/capnp-generated-fixture"]
input = ["pinned wire CodeGeneratorRequest", "pinned evolution-v2 CodeGeneratorRequest", "pinned import CodeGeneratorRequest"]
generated = ["typed struct and group readers/builders", "typed enums with collision-safe unknown ordinals", "typed recursive list readers", "union Which values with unknown discriminants", "source documentation", "stable type IDs", "import metadata"]
defaults = ["scalar XOR defaults", "pointer defaults", "absent evolved fields"]
cpp_to_rust = "generated typed reader decodes the pinned C++ wire frame"
rust_to_cpp = "pinned C++ decodes a 360-byte standard frame emitted through generated Rust builders"
negative = ["unknown requested file", "Rust keyword escaping", "identifier collision disambiguation", "unknown enum ordinal", "unknown union discriminant"]
scope = "generic surface APIs, constants, annotations, interfaces, and pipelines remain M20-M21"
validation = "Rust 1.98 and 1.85 workspace tests; generated-module compilation; public API docs; Clippy -D warnings; pinned C++ decode; Bazel complete test suite"

[evidence.M18]
crates = ["crates/capnp-schema", "crates/capnp-message"]
ownership = ["Arc-owned compiled schema", "Arc-owned message segments", "checked retained pointer and struct-list coordinates", "no leaked schema lifetime or global registry"]
read = ["scalar defaults", "Text and Data", "structs and groups", "all list encodings", "union activity", "known and unknown enums", "capabilities", "AnyPointer", "generic brand substitution", "aggregate schema constants"]
write = ["scalars", "Text and Data", "structs and groups", "primitive and pointer lists", "inline-composite struct lists", "nested lists", "enums", "capabilities"]
agreement = "dynamic and generated-style low-level views observe identical wire values"
safety = ["dynamic views are Send and Sync", "typed downcasts validate schema IDs", "exclusive child builders fail to alias at compile time"]
validation = "Rust 1.98 and 1.85 workspace tests; public API and compile-fail doc tests; Clippy -D warnings; Bazel complete test suite"

[evidence.M17]
crate = "crates/capnp-schema"
oracle_schema = "conformance/upstream/capnproto/e7c9cd96f1505b5ae486db7821006c2f5dce5b5b/schema.capnp"
oracle_fixtures = ["compiler-request-evolution-v1.bin", "compiler-request-evolution-v2.bin", "compiler-request-evolution-v3.bin", "compiler-request-import-fixture.bin", "compiler-request-language-fixture.bin", "compiler-request-streaming-fixture.bin", "compiler-request-wire-fixture.bin"]
model = ["nodes and node kinds", "fields and enumerants", "interfaces and methods", "recursive types", "generic brands", "all value discriminants", "annotations and targets", "source info", "requested files, imports, and source identifiers"]
lookups = ["node by ID", "nested declaration by parent and name", "field by name or stable index", "enumerant by name", "method by name", "source info by node ID", "requested file by ID"]
limits = ["standard frame segments and words", "exact traversal budget", "nesting depth", "aggregate metadata item count", "UTF-8 and union discriminant validation"]
negative = ["empty and trailing frames", "unknown discriminants", "duplicate node, source-info, and requested-file IDs", "invalid display-name prefixes", "missing group and source references", "source-member count mismatches", "item-limit exhaustion"]
scope = "pointer-backed list, struct, and any-pointer values retain their validated wire kind; dynamic interpretation is M18"
validation = "all seven pinned CodeGeneratorRequest fixtures; Rust 1.98 and 1.85 workspace tests; public API docs; Clippy -D warnings; Bazel 18/18"

[evidence.M16]
crates = ["crates/capnp-wire", "crates/capnp-message", "crates/capnp-io", "crates/capnp-async"]
sync = ["clean EOF distinct from truncation", "every partial read size with injected interruptions", "partial bounded writes", "over-limit writes rejected before mutation"]
async = ["executor-neutral Poll traits", "reader state retained across every injected Pending boundary", "ordered concatenated frames without overread", "exact unsent-byte backpressure", "ordered partial writes retained across cancellation"]
storage = ["caller-provided segment descriptor buffer with no allocation", "segment bytes borrow the original frame", "generic stable mmap-compatible backing with pointer-identical segment slices"]
matrix = ["capnp-wire no_std no allocation", "capnp-message no_std+alloc", "capnp-io no features", "capnp-io alloc", "capnp-io std", "capnp-async std"]
matrix_check = "tools/verify-m16-feature-matrix.sh"
validation = "Rust 1.98 and 1.85 workspace tests and feature checks; public API docs; Clippy -D warnings; Bazel 18/18"

[evidence.M15]
crate = "crates/capnp-io"
oracle_fixtures = ["wire-unpacked.bin", "wire-packed.bin"]
oracle_check = "tools/verify-m15-packed.sh: native output is byte-identical to pinned C++; C++ decodes native and native decodes C++"
streaming = ["arbitrary encoder byte chunks", "arbitrary decoder byte chunks", "partial words retained until finish", "zero and raw runs capped at 255 words"]
limits = ["checked output bound before every extension", "unaligned encoder input rejected", "truncated tag payloads and run bodies rejected", "failed streams become terminal"]
properties = ["all pinned C++ simple vectors", "every chunk size across the 800-byte oracle frame", "400 deterministic randomized round trips", "512 arbitrary packed inputs terminate within bounds"]
validation = "Rust 1.98 and 1.85 tests; public API docs; live pinned C++ interoperability; Clippy -D warnings; Bazel 18/18"

[evidence.M14]
crate = "crates/capnp-message"
oracle_fixture = "conformance/fixtures/cpp/e7c9cd96f1505b5ae486db7821006c2f5dce5b5b/builder-canonical.bin"
oracle_check = "tools/verify-m14-canonical.sh: native output is byte-identical to pinned C++ canonical output; C++ canonicalization is idempotent"
canonical_rules = ["exactly one segment", "dense pointer preorder without far pointers", "minimal struct data and pointer sections", "exact common inline-composite element width", "zero primitive and bit-list padding", "no capability pointers", "no unconsumed trailing words"]
properties = ["semantic values preserved across multi-segment input", "normalization is idempotent", "aliases duplicate into preorder", "traversal, nesting, and output allocation remain independently bounded"]
validation = "Rust 1.98 and 1.85 tests; public API docs; pinned C++ byte comparison and canonical acceptance; Clippy -D warnings; Bazel 18/18"

[evidence.M13]
crate = "crates/capnp-message"
oracle_schema = "conformance/schemas/orphan-fixture.capnp"
oracle_check = "tools/verify-m13-cpp-decode.sh: pinned C++ decodes zero-copy moved struct/list fields with exact expected values"
copy = ["schema-independent structs", "primitive and pointer lists", "inline-composite lists", "capabilities", "multi-segment sources and destinations", "transactional rollback on failure"]
clear = ["whole graph planned before mutation", "objects, pointer words, and far pads zeroed", "budget or nesting failure leaves graph byte-identical", "dropped orphan descendants zeroed iteratively"]
graph_limits = ["cycles terminate through traversal or nesting budgets", "overlapping targets charge per reference", "allocation failures roll back every new segment and byte"]
orphans = ["typed struct and list disown/adopt", "same backing coordinates preserved", "wrong kind rejected before detach", "foreign arena rejected", "exclusive orphan lifetime compile test"]
validation = "Rust 1.98 and 1.85 tests; public API and compile-fail doc tests; pinned C++ orphan decode; schema syntax; Clippy -D warnings; Bazel 18/18"

[evidence.M12]
crate = "crates/capnp-message"
oracle_schema = "conformance/schemas/builder-fixture.capnp"
oracle_check = "tools/verify-m12-cpp-decode.sh: pinned C++ decodes the native 360-byte standard multi-segment frame with exact expected values"
pad_coverage = ["same-segment positional pointers", "single-far struct and list landing pads", "double-far struct and list landing pads", "far root and nested pointers"]
limits = ["checked total-word limit", "checked segment-count limit", "per-segment signed-offset span", "checked far landing-pad offsets"]
determinism = "identical allocation operations under a fixed tiny-segment policy produce byte-identical segment vectors"
validation = "Rust 1.98 and 1.85 tests; public API and compile-fail doc tests; pinned C++ standard-frame decode; Clippy -D warnings; Bazel 18/18"

[evidence.M11]
crate = "crates/capnp-message"
oracle_schema = "conformance/schemas/builder-fixture.capnp"
oracle_check = "tools/verify-m11-cpp-decode.sh: pinned C++ decodes the native 248-byte flat message with exact expected values"
coverage = ["root struct and list initialization", "growth from a one-word reservation", "default-XOR scalar writes", "all primitive list encodings", "Text and Data", "nested structs", "pointer lists", "nested lists", "inline-composite struct lists", "capability pointer emission", "empty and zero-sized allocations", "bounds, size, and allocation limits"]
safety = ["all allocation growth is zero-initialized", "typed word offsets instead of native pointers", "child builders exclusively reborrow the arena", "aliasing builders rejected by compile-fail test", "failed limits do not expose partial allocations"]
validation = "Rust 1.98 and 1.85 tests; public API and compile-fail doc tests; pinned C++ flat decode; schema syntax; Clippy -D warnings; Bazel 18/18"

[evidence.M10]
crate = "crates/capnp-message"
ownership = ["borrowed caller-backed messages", "Arc-owned immutable segments", "typed struct and list roots", "retained struct and list references by checked coordinates", "short-lived callback readers"]
safety = ["Send and Sync derived from representation", "sealed wire-kind markers", "unchecked ObjectRef construction rejected at compile time", "borrowed data and callback readers cannot escape", "wrong wire kinds rejected before exposure"]
concurrency = "cloned object references share one exact atomic traversal budget; competing reads cannot overspend"
copying = "Arc segment inputs and retained subobjects preserve the original backing allocation"
validation = "Rust 1.98 and 1.85 tests; public API and compile-fail doc tests; Loom shared-budget model; Clippy -D warnings; Bazel 18/18"

[evidence.M09]
crate = "crates/capnp-message"
oracle_fixtures = ["wire-unpacked.bin", "evolution-v1-unpacked.bin", "evolution-v2-unpacked.bin"]
coverage = ["Void, Bit, Byte, TwoBytes, FourBytes, EightBytes, Pointer, and InlineComposite encodings", "all primitive widths and floating-point payloads", "known and unknown enum ordinals", "text and data pointer lists", "nested lists", "inline and zero-sized structs", "pointer-list to struct upgrade", "primitive and pointer views of struct lists", "null lists", "bounds and incompatible upgrades"]
accounting = "indexing and iteration produce identical traversal charges"
validation = "Rust 1.98 and 1.85 tests; public API doc tests; Loom shared-budget model; Clippy -D warnings; Bazel 18/18"

[evidence.M08]
crate = "crates/capnp-message"
oracle_fixtures = ["evolution-v1-unpacked.bin", "evolution-v2-unpacked.bin"]
cross_read = "pinned C++ v2 decodes v1 with absent defaults; pinned C++ v1 decodes v2 while preserving unknown enum ordinal 2"
coverage = ["data and pointer sections", "short-section defaults", "old/new cross-read both directions", "groups share storage", "known and unknown union tags", "nested structs", "pointer defaults charged under traversal and nesting limits"]
validation = "Rust 1.98 and 1.85 tests; public API doc tests; Clippy -D warnings; C++ fixture hashes; Bazel 18/18"

[evidence.M07]
crate = "crates/capnp-message"
oracle_fixture = "conformance/fixtures/cpp/e7c9cd96f1505b5ae486db7821006c2f5dce5b5b/wire-unpacked.bin"
coverage = ["all scalar kinds", "default XOR", "least-significant bool bits", "known and unknown enums", "null blobs", "Text terminal NUL", "deferred UTF-8", "interior NUL", "Data bytes", "zero-copy borrowing", "charged blob views"]
property = "10,000 deterministic u64 wire/default pairs preserve exact XOR semantics"
validation = "Rust 1.98 and 1.85 tests; public API doc tests; Clippy -D warnings; Bazel 18/18"

[evidence.M06]
crate = "crates/capnp-message"
coverage = ["complete-or-unchanged local charges", "exact atomic shared charges", "zero-sized-list amplification", "far landing pads", "cycles", "overlaps", "copied nesting", "50,000-level iterative depth"]
concurrency = "Loom exhaustively models competing charges; eight-thread stress preserves the exact initial balance"
benchmark = "checked-in local/shared charge comparison; 2026-08-30 Linux container sample: 1.60 ns local, 8.57 ns shared per charge"
validation = "Rust 1.98 and 1.85 tests; public API doc/compile-fail tests; Clippy -D warnings; Bazel 18/18"

[evidence.M05]
crate = "crates/capnp-message"
coverage = ["null", "struct", "all list sizes", "inline composite", "single far", "double far", "capability", "reserved", "malformed randomized words"]
validation = "Rust 1.98 and 1.85 tests; Clippy -D warnings; Bazel 18/18"

[evidence.M04]
crate = "crates/capnp-io"
oracle_fixtures = ["wire-unpacked.bin", "wire-two-segment.bin", "wire-multisegment.bin"]
oracle_segment_counts = [1, 2, 33]
coverage = ["clean EOF", "table padding", "truncation", "limits", "overflow", "concatenated frames", "exact re-encoding"]
validation = "Rust 1.98 and 1.85 tests; Clippy -D warnings; Bazel 18/18"

[evidence.M03]
crate = "crates/capnp-wire"
oracle_fixture = "conformance/fixtures/cpp/e7c9cd96f1505b5ae486db7821006c2f5dce5b5b/wire-unpacked.bin"
coverage = ["scalar bytes", "NaN payloads", "checked arithmetic", "signed 30-bit offsets", "pointer bitfields", "unaligned byte paths"]
validation = "Rust 1.98 and 1.85 tests; Clippy -D warnings; Bazel 18/18"

[evidence.M01]
cpp_fixtures = "conformance/fixtures/cpp/e7c9cd96f1505b5ae486db7821006c2f5dce5b5b"
rust_fixtures = "conformance/fixtures/capnproto-rust/2228b71e55cee819c30450bb9bfd9c1f6a722429"
baseline = "benchmarks/results/2026-08-30-g-drive-docker"
covered_baselines = ["build/read", "unpacked", "packed", "two-party RPC"]
rpc_baseline = "benchmarks/results/2026-08-30-g-drive-docker/rpc-summary.tsv"

[parallel_gates]
four_core_shared_read_ratio = 3.0
four_core_rpc_handler_ratio = 3.0
four_core_partitioned_build_ratio = 2.5
maximum_unexplained_single_thread_regression_percent = 5.0
